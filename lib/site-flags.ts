// Banderas de visibilidad del sitio público (landing).
// Cambiar a `true` cuando el pricing esté definido y se quiera volver a mostrar.
// El código de la sección de Planes se mantiene intacto; solo no se renderiza.
export const SHOW_PRICING = false;
